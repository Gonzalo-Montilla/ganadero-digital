# Copiar en Railway → servicio backend → Variables
# DATABASE_URL: enlazar desde el plugin PostgreSQL (${{Postgres.DATABASE_URL}})

SECRET_KEY=genera-clave-larga-aleatoria
ALGORITHM=HS256
DEBUG=False
ENVIRONMENT=staging
ALLOW_PUBLIC_REGISTRATION=false
ENABLE_BOOTSTRAP_SEED=true
NOTIFICATIONS_ENABLED=false

# Actualizar con la URL EXACTA del frontend (sin / al final)
BACKEND_CORS_ORIGINS=https://zesty-luck-production-8eb4.up.railway.app
