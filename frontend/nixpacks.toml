[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node scripts/write-runtime-config.cjs && npx vite preview --host 0.0.0.0 --port $PORT"
